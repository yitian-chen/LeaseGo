<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zju.lease.agent.mapper.RoomAttrValueMapper">
    <select id="selectListByRoomId" resultType="com.zju.lease.model.entity.AttrValue">
        select av.id,
               av.name,
               av.attr_key_id
        from attr_value av
        where av.is_deleted = 0
          and av.id in (select attr_value_id
                        from room_attr_value
                        where is_deleted = 0
                          and room_id = #{roomId})
    </select>
</mapper>
