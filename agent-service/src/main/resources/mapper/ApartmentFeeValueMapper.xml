<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zju.lease.agent.mapper.ApartmentFeeValueMapper">
    <select id="selectListByApartmentId" resultType="com.zju.lease.model.entity.FeeValue">
        select fv.id,
               fv.name,
               fv.unit,
               fv.fee_key_id
        from fee_value fv
        where fv.is_deleted = 0
          and fv.id in (select fee_value_id
                        from apartment_fee_value
                        where is_deleted = 0
                          and apartment_id = #{apartmentId})
    </select>
</mapper>
