<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zju.lease.agent.mapper.ApartmentFacilityMapper">
    <select id="selectListByApartmentId" resultType="com.zju.lease.model.entity.FacilityInfo">
        select fi.id,
               fi.name,
               fi.type
        from facility_info fi
        where fi.is_deleted = 0
          and fi.id in (select facility_id
                        from apartment_facility
                        where is_deleted = 0
                          and apartment_id = #{apartmentId})
    </select>
</mapper>
