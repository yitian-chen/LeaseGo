<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zju.lease.web.app.mapper.ChatConversationReadMapper">

    <select id="selectByUserIdAndConversationId" resultType="com.zju.lease.model.entity.ChatConversationRead">
        SELECT id, user_id, conversation_id, last_read_time, unread_count, create_time, update_time, is_deleted
        FROM chat_conversation_read
        WHERE user_id = #{userId} AND conversation_id = #{conversationId} AND is_deleted = 0
    </select>
</mapper>
